<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:standard="using:MVVMSample.Standard">

    <!--<DataTemplate x:Key="PersonItemTemplate" x:DataType="standard:PersonViewModel">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding Name}" />
            <TextBlock Foreground="{Binding GenderBrush}" Text="{Binding GenderString}" />
        </StackPanel>
    </DataTemplate>-->

</ResourceDictionary>
